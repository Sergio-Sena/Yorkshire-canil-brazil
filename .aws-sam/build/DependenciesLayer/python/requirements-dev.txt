# Dependências de desenvolvimento/teste — NÃO incluir no Lambda
-r requirements.txt

pytest==8.2.0
pytest-cov==5.0.0          # cobertura de código
moto[dynamodb,sqs,sns]==5.0.0  # mock AWS local
freezegun==1.5.0            # congela datetime para testar horário noturno
